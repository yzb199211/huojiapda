<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rl_top"
    android:layout_width="match_parent"
    android:layout_height="@dimen/public_dp_50"
    android:background="@color/pubilc_blue"
    android:clickable="true">

    <ImageView
        android:id="@+id/iv_back"
        android:layout_width="@dimen/public_dp_50"
        android:layout_height="match_parent"
        android:padding="@dimen/public_dp_12"
        android:src="@mipmap/icon_back_white"
        android:visibility="gone" />

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:textColor="@color/white"
        android:textSize="@dimen/public_sp_16"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/tv_right"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:gravity="center"
        android:padding="@dimen/public_dp_12"
        android:text="筛选"
        android:textColor="@color/white"
        android:textSize="@dimen/public_sp_12"
        android:visibility="gone"
        />

    <View
        android:id="@+id/top_line"
        android:layout_width="match_parent"
        android:layout_height="@dimen/public_dp_1"
        android:layout_alignParentBottom="true"
        android:background="@color/public_default_bg_color"
        android:visibility="gone" />

</RelativeLayout>
